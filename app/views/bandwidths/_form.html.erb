<%= form_with(model: bandwidth, local: true) do |form| %>
  <% if bandwidth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bandwidth.errors.count, "error") %> prohibited this bandwidth from being saved:</h2>

      <ul>
        <% bandwidth.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :server_id %>
    <%= form.text_field :server_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :interface_name %>
    <%= form.text_area :interface_name %>
  </div>

  <div class="field">
    <%= form.label :value_id %>
    <%= form.text_field :value_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
